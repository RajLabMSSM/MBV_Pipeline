__default__:
  queue: premium
  cores: 1
  mem: 3000
  time: '600'
  name: $(basename $(pwd)):{rule}:{wildcards}
  output: logs/{rule}:{wildcards}.stdout
  error: logs/{rule}:{wildcards}.stderr
  himem: ""
matchBAM2VCF:
    cores: 4
    mem: 3000
    time: 600
